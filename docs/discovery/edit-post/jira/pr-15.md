---
ticket: EDIT-POST-15
design-status: ready but only as a generic idea. Will not be designed further.
type: Task
title: "Edit Post Page — loading skeleton"
dependencies: [EDIT-POST-9]
blocks: []
---

> **Skeleton install + discovery required before implementation.** Run `npx shadcn add skeleton` and split into `globals/components/ui/skeleton/` to match existing conventions. The Figma skeleton (screen 3, node 527:1568) shows `bg-border` rectangles as a starting point only — actual region shapes, heights, and spacing must be determined at implementation time to match the final page layout.

## Description

Add a loading skeleton to the edit page that matches the completed page layout.
This PR is intentionally deferred until after the action bar and toolbar land
in PR 9 so the skeleton reflects the final layout without rework.

The skeleton replaces the current `<p data-testid="edit-post-loader">Loading
post</p>` `Suspense` fallback in `page.tsx`.

**Skeleton regions to cover:**
- Action bar area (sticky bar with toolbar placeholder + button placeholders)
- Title input area
- Editor body area

## Acceptance Criteria

- [ ] The `Suspense` fallback in `page.tsx` renders a skeleton instead of a plain text placeholder
- [ ] The skeleton covers the action bar, title, and editor regions
- [ ] The skeleton does not cause layout shift when the real content loads
- [ ] `pnpm validate` passes

## Files

| Action | Path |
|--------|------|
| Created | `features/posts/components/editPostSkeleton/editPostSkeleton.tsx` |
| Modified | `app/posts/[id]/edit/page.tsx` (swap fallback) |
