---
ticket: EDIT-POST-1
type: Task
title: "Rename RichTextEditor → LegacyRichTextEditor; update all consumers"
dependencies: []
blocks: [EDIT-POST-7, EDIT-POST-8, EDIT-POST-9]
---

## Description

Rename the existing `RichTextEditor` component to `LegacyRichTextEditor` and update every consumer. No behavior changes.

This rename must land before any edit page work (PRs 7–9) touches the `RichTextEditor` name, because PR 9 introduces a new `RichTextEditor` that differs from the existing one (no internal `LexicalComposer`, no embedded toolbar). Without this rename, consumers of the old component would break. See `decisions.md` → D21.

**Scope:**
- Rename the component file/directory and the export name.
- Find and update all import sites across the codebase.
- No logic, styling, or API changes.

## Acceptance Criteria

- [ ] `RichTextEditor` → `LegacyRichTextEditor` everywhere (file names, exports, imports)
- [ ] All existing consumers compile and render identically to before
- [ ] No behavior changes — this is a rename-only PR
- [ ] `pnpm tsc` passes with zero errors
- [ ] `pnpm validate` passes

## Technical Notes

- Use a global search-and-replace to find all `RichTextEditor` references, then audit for false positives (e.g. `LexicalComposer` internals that happen to mention the name).
- After this PR, `RichTextEditor` is a free name — PR 9 creates a new component with that name.
- See `decisions.md` → D21 for the rationale behind naming the existing component `Legacy`.

## Testing

No new tests. Existing tests confirm no regression — run `pnpm test:coverage` to verify.

## Files

| Action | Path |
|--------|------|
| Renamed | `globals/components/richTextEditor/` → all exports renamed to `LegacyRichTextEditor` |
| Modified | All files importing `RichTextEditor` from the old path |
