---
ticket: EDIT-POST-10
type: Task
title: "Description button + modal, Close button (flush + redirect + no-title confirmation)"
dependencies: [EDIT-POST-2, EDIT-POST-9]
blocks: []
---

## Description

Add the Description and Close actions to the `ActionBar`.

**`DescriptionButton` + `DescriptionModal`:**
- `DescriptionButton` lives in `ActionBar` and opens the Shadcn `Dialog`.
- `DescriptionModal` holds temporary local state (`localDescription`) initialised from `EditPostClient.description` when the modal opens.
- **Save:** calls `updatePost` with current form fields + `localDescription` → on success updates `EditPostClient.description` + calls `cancelPendingDebounce` + closes the modal. Inline error shown in the modal on failure.
- **Cancel:** discards temp state and closes the modal without saving. Autosave does not fire.

**`CloseButton`:**

On click:
1. Cancel any pending autosave debounce.
2. Call `updatePost` directly (flush) with current field state.
3. **On success:** redirect to `ROUTES.post(id)`.
4. **On failure with no title:** show delete confirmation `Dialog`. If admin confirms deletion, delete the post and redirect to `ROUTES.posts`.
5. **On failure with a title:** Sonner error toast; admin stays on the edit page.

See `decisions.md` → D10 (Close destination) and D13 (no-title confirmation flow).

## Acceptance Criteria

- [ ] `DescriptionButton` in `ActionBar` opens `DescriptionModal`
- [ ] `DescriptionModal` contains a `<textarea>` pre-filled with current description
- [ ] `DescriptionModal` has a Save button and a Cancel button
- [ ] Save calls `updatePost` with current title, content, and the modal's local description value
- [ ] Save on success: updates `EditPostClient.description`, calls `cancelPendingDebounce`, closes modal
- [ ] Save on failure: shows inline error inside the modal; modal stays open
- [ ] Cancel discards local description state and closes the modal without saving
- [ ] Autosave does not fire on modal close (Save or Cancel path)
- [ ] `CloseButton` cancels pending debounce before flushing
- [ ] `CloseButton` calls `updatePost` directly on click (flush)
- [ ] On flush success: redirects to `ROUTES.post(id)`
- [ ] On flush failure with no title: shows delete confirmation `Dialog`
- [ ] Confirming deletion in the dialog: deletes the post and redirects to `ROUTES.posts`
- [ ] On flush failure with a title: shows Sonner error toast; admin stays on edit page
- [ ] `pnpm validate` passes

## Technical Notes

- `CloseButton` uses `flushPendingDebounce` from `useAutoSave` (exposed by `EditPostClient`), not a raw `updatePost` call with a hardcoded timeout.
- The no-title confirmation dialog uses the Shadcn `Dialog` from PR 2.
- Sonner is only used for Close failures that have a title. No-title failures use the confirmation dialog instead (deleting a no-title draft is non-destructive — it was never a real post). See `decisions.md` → D13.

## Testing

| Subject | Project | Cases |
|---------|---------|-------|
| `CloseButton` | `default` | Calls `flushPendingDebounce` on click; shows confirmation dialog on no-title failure |
| `DescriptionModal` | `default` | Opens on button click; Save calls `updatePost` and closes; Save failure shows inline error; Cancel closes without saving |

## Files

| Action | Path |
|--------|------|
| Created | `features/posts/components/descriptionButton/descriptionButton.tsx` |
| Created | `features/posts/components/descriptionModal/descriptionModal.tsx` |
| Created | `features/posts/components/closeButton/closeButton.tsx` |
| Modified | `features/posts/components/actionBar/actionBar.tsx` (add Description + Close slots) |
